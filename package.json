{
  "private": true,
  "name": "react-i",
  "scripts": {
    "start": "npm start",
    "build": "npm build",
    "predeploy": "npm build",
    "deploy": "npm deploy --prod --dir=build",
    "setup-deploy": "app-app --deploy && npm deploy",
    "test": "npm test",
    "eject": "npm eject",
    "postdeploy": "echo 'Your site was deployed to https://react-i-danteharasz.netlify.com'"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "stylelint": "^13.8.0",
    "stylelint-config-standard": "^20.0.0"
  },
  "dependencies": {
    "json": "^10.0.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "^4.0.0"
  },
  "homepage": "https://react-i-danteharasz.netlify.com"
}
